const config = {
  networkName: 'Turing Staging Network',
  tokenSymbol: 'TUR',
  units: 10**10,
  dripActions: {
    normal: {
      amount: 100,
      timesLimit: 1,
    },
  },
};

module.exports = config;
